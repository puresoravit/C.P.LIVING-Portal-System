# Database
DATABASE_URL="postgresql://user:password@localhost:5432/bill_system?schema=public"

# NextAuth
NEXTAUTH_SECRET="เปลี่ยนเป็นค่าสุ่มยาวๆ (openssl rand -base64 32)"
NEXTAUTH_URL="http://localhost:3000"

# Backup (ข้อ 49) — ใช้เป็น token ป้องกัน endpoint /api/backup/auto
BACKUP_SECRET="เปลี่ยนเป็นค่าสุ่มยาวๆ อีกค่าหนึ่ง"
